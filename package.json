{
  "name": "mmmt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "ENVIRONMENT=production rm -rf dist && npx @11ty/eleventy",
    "start": "ENVIRONMENT=development rm -rf dist && npx @11ty/eleventy --watch --serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "@11ty/eleventy-img": "^2.0.0",
    "@grimlink/eleventy-plugin-sass": "^1.0.2",
    "date-format": "^4.0.7",
    "sass": "^1.50.1"
  }
}
